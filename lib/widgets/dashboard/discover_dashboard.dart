import 'package:flutter/material.dart';

class DiscoverDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is Discovery'),
    );
  }
}
